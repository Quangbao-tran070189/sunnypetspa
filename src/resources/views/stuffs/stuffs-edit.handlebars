<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chỉnh Sửa Thông Tin Sản Phẩm Phụ Kiện</title>
    <style>
        .mt-4 {
            margin-top: 1.5rem;
        }
        
        .row {
            display: flex;
            flex-wrap: wrap;
            margin-right: -15px;
            margin-left: -15px;
        }
        
        .mb-3 {
            margin-bottom: 1rem;
        }
        
        .form-label {
            font-weight: bold;
        }
        
        .form-control {
            margin-bottom: 10px;
        }
        
        .img-fluid {
            max-width: 100px; /* Đặt kích thước hình ảnh nhỏ hơn */
            height: auto;
            display: block;
            margin-top: 10px;
        }
        
        .row .col-md-6 {
            flex: 0 0 50%;
            max-width: 50%;
            padding-right: 15px;
            padding-left: 15px;
        }
        
        .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
            color: white;
        }
        
        .btn-primary:hover {
            background-color: #0056b3;
            border-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="mt-4">
        <div class="row">
            <h2>Chỉnh Sửa Thông Tin Sản Phẩm Phụ Kiện</h2>
            <form method="POST" action="/stuffs/{{stuff._id}}?_method=PUT" enctype="multipart/form-data">
                <div class="mb-3">
                    <label for="name" class="form-label">Tên Sản Phẩm</label>
                    <input type="text" class="form-control" value="{{stuff.name}}" id="name" name="name">
                </div>
                <div class="mb-3">
                    <label for="description" class="form-label">Mô tả sản phẩm</label>
                    <textarea class="form-control" id="description" name="description">{{stuff.description}}</textarea>
                </div> 
                <div class="row mb-3">
                    <div class="col-md-6">
                        <label for="price" class="form-label">Giá sản phẩm</label>
                        <input type="text" class="form-control" value="{{stuff.price}}" id="price" name="price">
                    </div>
                    <div class="col-md-6">
                        <label for="origin" class="form-label">Xuất xứ</label>
                        <input type="text" class="form-control" value="{{stuff.origin}}" id="origin" name="origin">
                    </div>
                </div> 
                <div class="mb-3">
                    <label for="image" class="form-label">Hình ảnh SP</label>
                    <input type="file" class="form-control" id="image" name="image">
                    <img src="/img/{{stuff.image}}" class="img-fluid mt-2" alt="Stuff Image">
                </div>   
                <button type="submit" class="btn btn-primary">Lưu lại</button>
            </form>
        </div>
    </div>
</body>
</html>
