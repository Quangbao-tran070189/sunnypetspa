

<div class="mt-4">
        <div class="row">
            <h3>Đây Là Trang Lưu Trữ Dược Phẩm Thú Y</h3>
            {{#each medicines}}
            <div class="col-sm-6 col-lg-2 card-separate">
                <div class="card mt-4 card-frame">
                    <div clas="image-frame">
                    <img src="/img/{{this.image}}" class="card-img-top card-image" alt="...">
                    </div>
                    <div class="card-body">
                        <h5 class="card-title card-name">{{this.name}}</h5>
                        <div class="price-space">
                          <div class="card-text card-price">{{this.price}}</div>
                          <div class="card-text card-origin">{{this.origin}}</div>
                        </div>
                        <p class="card-text card-description">{{this.description}}</p>
                    </div>
                    <li><hr class="dropdown-divider"></li>
                    <div class="btn-fix-separate">
                        <a class="btn btn-primary " href="/medicines/{{this._id}}/medicine-edit" role="button">Sửa</a>
                        <a class="btn btn-primary " href="#" role="button" data-bs-toggle="modal" data-bs-target="#Medicine-Delete-Modal" data-id="{{this._id}}">Xóa</a>
                    </div>
                    
                </div>
            </div>
           {{/each}}
        </div>
</div>

<div class="modal fade" id="Medicine-Delete-Modal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h1 class="modal-title fs-5" id="exampleModalLabel">Xác nhận xóa?</h1>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        ...
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Không Xóa</button>
        <button type="button" class="btn btn-danger" id="btn-delete-medicine">Có xóa</button>
      </div>
    </div>
  </div>
</div>

<!--Delete Hidden Form -->

<form method="POST" name="delete-medicine-form"></form>

<script>

document.addEventListener('DOMContentLoaded', function() {
    var medicineId;
    var deleteForm = document.forms['delete-medicine-form'];

    //when dialog confirm clicked
     $('#Medicine-Delete-Modal').on('show.bs.modal', function (event) {
        var button = $(event.relatedTarget); // Button that triggered the modal
        medicineId = button.data('id'); // Extract info from data-* attributes
        //console.log(medicineId);
        //console.log('DOM fuly load');
    });
   
var btnDeleteMedicine = document.getElementById('btn-delete-medicine');
    //console.log(btnDeletestuff);

    // When Delete btn click
btnDeleteMedicine.onclick = function () {
    deleteForm.action = '/medicines/' + medicineId + '?_method=DELETE';
    deleteForm.submit();
}
});

    
</script>